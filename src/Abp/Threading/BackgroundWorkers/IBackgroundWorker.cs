namespace Abp.Threading.BackgroundWorkers
{
    /// <summary>
    /// Interface for a worker (thread) that runs on background to perform some tasks.
    /// 在后台运行以执行某些任务的工作者（线程）的接口
    /// </summary>
    public interface IBackgroundWorker : IRunnable
    {

    }
}
